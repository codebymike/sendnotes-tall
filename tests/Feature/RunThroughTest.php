<?php

/*
Set up a product comfig
pull and order from shopify - turn it into orderlines
generate WFs from those orderlines (match product)
generate artworks from those wfs
(mark those artworks as complete)
check they appear in the queue - mark them as ready
check the wfs are marked as ready
mark as sent to supplier
generate a supplier update (check URL is active)
check that that update is process and WFs are updated
*/

test('product setup', function () {

});